apiVersion: v1
kind: Secret
metadata:
  name: safety-service-secrets
  namespace: dev
type: Opaque
stringData:
  # Database Credentials
  DATABASE_USERNAME: "tripo04os_user"
  DATABASE_PASSWORD: "dev_password_change_me"

  # Emergency Services API Key
  EMERGENCY_API_KEY: "dev_emergency_api_key_change_me"

  # AWS Credentials
  AWS_ACCESS_KEY_ID: "dev_aws_access_key"
  AWS_SECRET_ACCESS_KEY: "dev_aws_secret_key"

  # JWT Secret for internal communication
  JWT_SECRET: "dev_jwt_secret_change_me"
