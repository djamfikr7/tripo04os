apiVersion: v1
kind: Secret
metadata:
  name: safety-service-secrets
  namespace: staging
type: Opaque
stringData:
  # Database Credentials
  DATABASE_USERNAME: "tripo04os_user"
  DATABASE_PASSWORD: "staging_password_change_me"

  # Emergency Services API Key
  EMERGENCY_API_KEY: "staging_emergency_api_key_change_me"

  # AWS Credentials
  AWS_ACCESS_KEY_ID: "staging_aws_access_key"
  AWS_SECRET_ACCESS_KEY: "staging_aws_secret_key"

  # JWT Secret for internal communication
  JWT_SECRET: "staging_jwt_secret_change_me"
