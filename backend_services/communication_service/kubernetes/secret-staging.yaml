apiVersion: v1
kind: Secret
metadata:
  name: communication-service-secrets
  namespace: staging
type: Opaque
stringData:
  # Database Credentials
  DATABASE_USERNAME: "tripo04os_user"
  DATABASE_PASSWORD: "staging_password_change_me"

  # JWT Secret for Socket.io authentication
  JWT_SECRET: "staging_jwt_secret_change_me"

  # WebSocket Secret
  WEBSOCKET_SECRET: "staging_websocket_secret_change_me"
